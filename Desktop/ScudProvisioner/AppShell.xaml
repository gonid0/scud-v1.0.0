<?xml version="1.0" encoding="UTF-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
       xmlns:views="clr-namespace:ScudProvisioner.Views"
       x:Class="ScudProvisioner.AppShell"
       FlyoutBehavior="Disabled"
       Title="SCUD Reader Provisioner">
    <TabBar>
        <ShellContent Title="Provision" ContentTemplate="{DataTemplate views:ProvisionPage}" Route="provision" />
        <ShellContent Title="Templates" ContentTemplate="{DataTemplate views:TemplatesPage}" Route="templates" />
        <ShellContent Title="Settings"  ContentTemplate="{DataTemplate views:SettingsPage}"  Route="settings" />
    </TabBar>
</Shell>
